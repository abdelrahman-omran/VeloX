VITE_USE_FIXTURES=false

# Dev proxy target for /api when fixtures are off
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Production API base URL (leave empty in dev to use the Vite proxy above)
VITE_API_BASE_URL=
